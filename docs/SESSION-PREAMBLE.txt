GROUND TRUTH — verified against source; do not re-derive or "improve":

CANONICAL JSON (AERF SPEC.md §5.1; matches .vendor/agentmint-python/agentmint/
notary.py _canonical_json and .vendor/aerf/verifiers/go/internal/aerf/canonical.go):
- Keys sorted by Unicode code point; separators "," and ":" (no whitespace)
- Strings NFC-normalized; escapes per RFC 8785 §3.2.2.2 ONLY: \" \\ \b \f \n \r \t,
  other control chars as \u00XX lowercase hex, RAW UTF-8 for everything else
  (never \uXXXX for printable non-ASCII)
- Numbers: producer THROWS on any non-integer finite number in a signed payload
  (JS cannot round-trip 1250.0; money = integer micro-units or strings).
  Verifier replays number lexemes verbatim from source bytes (like Go json.Number).

SIGNING: Ed25519 via node:crypto only — generateKeyPairSync("ed25519"),
sign(null, bytes, key), verify(null, bytes, pub, sig). Keys as SPKI PEM (RFC 8410).
Raw public key = last 32 bytes of SPKI DER.
key_id = first 16 hex chars of SHA-256(raw 32-byte public key)
(mirror _derive_key_id in .vendor/agentmint-python/agentmint/notary.py).

SIGNED PAYLOAD = receipt object MINUS post-issuance fields:
signature, timestamp, parent_signature, parent_key_id, log_inclusion_proof
(mirror postIssuanceFields in .vendor/aerf/verifiers/go/internal/aerf/verify.go).

CHAIN: previous_receipt_hash = SHA-256 hex of canonical bytes of the previous
receipt INCLUDING its "signature" field. Genesis receipt OMITS the field
entirely (not null).

HASHES: params_hash = SHA-256 hex of canonical(params).
spec_hash / policy_hash = SHA-256 hex of canonical(policy object).
evidence_hash_sha512 = SHA-512 hex of canonical(evidence), when evidence is used.

REPO CONSTRAINTS: zero runtime dependencies (node:crypto only), Node >=18,
ESM imports use .js extensions, src/kernel/ never imports from
src/experimental/ and never from .vendor/.

ORACLE: test/aerf-verify-poc.mjs verifies all 12 vectors in test/vectors/ with
these exact semantics. 12/12 is the acceptance bar. Never modify the oracle.
